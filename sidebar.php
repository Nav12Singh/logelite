<?php
/**
 * Sidebar template.
 *
 * Currently unused: this theme's only registered widget areas are the
 * four footer columns (lgl-footer-1..4, see inc/setup.php's
 * lgl_widgets_init()), rendered directly by template-parts/footer/
 * site-footer.php, not through a blog sidebar. No template in this theme
 * calls get_sidebar(). Kept as an empty, valid file only because CLAUDE.md
 * §3 lists it as part of the expected theme file structure.
 *
 * @package logelite
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
